/*const multiply= (a, b) => {

    console.log(a*b);
}
    
multiply(4,8);
multiply(4,0);*/


/*const add= (a, b) => {

   return a+b;
}
    console.log(add(1,2));*/

    const multiparameters=(a,b)=>{
        const add=a+b;
        const sub=a-b;
        const mul=a*b;

        console.log(`add:${add}`);
        console.log(`sub:${sub}`);
        console.log(`mul:${mul}`);
    }
    multiparameters(3,10);
    multiparameters(3,15);







